package week12;

public class ClubMembers {
    /*
     * private ArrayList<MemberInfo> memberList;
     * 
     * public void addMembers(String[] names, int gradYear) {
     * for (String name : names) {
     * MemberInfo mi = new MemberInfo(name, gradYear);
     * memberList.add(mi);
     * }
     * }
     * 
     * public ArrayList<MemberInfo> removeMembers(int year) {
     * ArrayList<MemberInfo> goodStanding = new ArrayList<MemberInfo>();
     * for (int i = memberList.size(); i >= 0; i--) {
     * if (memberList.get(i).getGradYear() <= year)
     * memberList.remove(i);
     * if (memberList.get(i).getGradYear() <= year &&
     * memberList.get(i).inGoodStanding() == true)
     * goodStanding.add(memberlist.get(i));
     * }
     * return goodStanding;
     * }
     */
}
